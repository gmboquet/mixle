# 0.8.0 Adversarial Systems Review — Protocol (Worklist E14.3)

**Status: protocol, not a completed review.** This is the instrument an independent
**systems** reviewer uses to challenge mixle's packaging, distributed execution,
checkpointing, and benchmark methodology. Execute it from a **clean clone** in a fresh
environment, as someone other than the implementer.

**Minimum for a review to count at all:** from that clean clone, reproduce
**at least one backend** and **at least one benchmark** end to end. Full acceptance is stricter --
*every* supported/optional-CI backend and *every* release-current benchmark -- but a review
that reproduces neither has established nothing about this candidate, and should be recorded
as not-started rather than as a pass with gaps. The floor and the bar are stated separately
on purpose: a partial review is useful evidence, an unexecuted one is not.

## Candidate identity

- Reviewed source commit (full 40-character SHA):
- Reviewed wheel filename and SHA-256:
- Reviewed sdist filename and SHA-256:
- Identity matches the release-owner candidate receipt: ☐ yes ☐ no

The clean checkout must detach at the exact source commit. A review against another
commit or rebuilt bytes cannot authorize this candidate.

**Acceptance (E14.3):** the reviewer completes the risk-based matrix below for every
surface claimed supported on the candidate. A skipped or unavailable cell must cause
the public support claim to be narrowed. Record outcomes in the findings table.

---

## 1. Packaging

**Checks.**
- `pip install mixle` in a fresh venv (base install, no extras). Does it import? Time
  `python -c "import mixle"` (the import-latency budget is gated separately).
- Install each extra group you can (`mixle[torch]`, `mixle[dask]`, ...). Does the
  resolver succeed on your platform? Note any platform-gated extras (e.g. TBB on arm64).
- Confirm the base install pulls no optional heavy dependency (torch, spark, ...).

**Reviewer question:** does the dependency floor match what the code actually uses, or
is it optimistic?

## 2. Distributed execution

**Checks.**
- Run `python examples/scaling_example.py`. It executes only `local` and multiprocessing
  and fails unless their fitted parameters agree exactly; it does not count as Dask, MPI,
  Spark, Ray, Lightning, or torchrun evidence.
- Reproduce every backend the 0.8.0 support matrix labels supported or optional-CI.
  Record the exact command and retained receipt for each route. Dask uses
  `mixle/tests/dask_encoded_data_test.py`; MPI uses the MPI-marked test under
  `mpiexec`; torchrun uses `torchrun_encoded_data_test.py` and
  `torch_neural_test.py`. A commented example line is not execution evidence.
- Confirm the distributed fit agrees with the local fit to tolerance (the MPI route
  equivalence and backend-matrix tests are the reference).

**Reviewer question:** does "scale with a flag" hold on your machine, and does the
backend agree with local numerically?

## 3. Checkpointing

**Checks.**
- Run `python examples/production_example.py` — it fits with provenance, registers a
  model, promotes an alias, and checks a drift report.
- Interrupt a long fit that uses `registry.checkpointer(...)` and confirm the chain
  verifies (`registry.verify_chain(...)`).
- Confirm a promoted alias resolves to a specific version, and that promotion is separate
  from registration.

**Reviewer question:** can you recover a fit from a checkpoint, and is the lineage honest
about what it does and does not record?

## 4. Benchmark methodology

**Checks.**
- Reproduce every release-current benchmark or measured report from the reproducibility bundle
  (`0.8.0-repro-bundle.json`) with
  `python scripts/run_repro_entry.py --entry <id>` — require its validated receipt and
  attach that receipt rather than comparing unstructured output by eye.
- Confirm performance claims state whether they are throughput, latency, or capability
  (see the performance-crossover page), and that losses (where a specialized package
  wins) are reported alongside wins.
- Confirm no headline number is carried over from a pre-0.8.0 artifact.

**Reviewer question:** are the benchmark numbers reproducible on your hardware within a
stated tolerance, and is the methodology honest about what it does not measure?

---

## Findings

Executed 2026-08-13 against candidate `32a53092` by an independent systems-review track.
**Disposition: NO-GO — 5 High, 3 Medium**, plus a process blocker (no owner-issued receipt bound the
reviewed byte pair, so the candidate-identity block above reads NO regardless of test outcomes).
The full report and its 234 supporting receipts are archived under
`~/mixle/review-archive/sysreview-32a53092/`.

Every finding is repaired at `a7820684` (D-0176), each reproduced before it was touched and each
fix shipping an adverse test in the same commit. **Repairing them does not close this gate**: the
acceptance below requires the matrix completed against the exact candidate, and these are
unreplayed repairs — two of the eight were themselves caught breaking CI, which is the argument for
replaying rather than trusting them.

| # | Severity | Area | Finding | Reproduced? | Resolution |
|---|----------|------|---------|-------------|------------|
| SYS-01 | High | Sdist identity | Ordinary sdist installation produced a wheel/runtime with commit/tree `unknown` and dirty `null` on both supported Pythons. | yes | `a2ea429e` — the sdist ships its build attestation and `build_py` carries it forward when git is absent, marked `source_content_carried_through_sdist`. |
| SYS-02 | High | Installed reproduction | The documented pip-install + `mixle-reproduce` route returned `passed:false`: 810 ordinary pip-generated unhashed `.pyc` RECORD rows were treated as tampering. | yes | `94dfd16a` — installer bytecode exempt only when it maps to a hashed distributed source; count published as `installer_bytecode_exempt_count`. |
| SYS-03 | High | Bundle evidence binding | `run_repro_entry.py` never opened `candidate_binding.required_records`, and its receipt omitted commit, artifact digest, installed distribution, and environment identity; source-shadowed mixle still received `claim_status:"verified"`. | yes | `6fb1d9b8` — receipts record `executing_artifact`, `candidate_binding`, `unbound_reasons`; "verified" requires an installed distribution whose content verifies plus resolved records. |
| SYS-04 | High | Checkpoint recovery | `Registry.checkpointer()` under the same name created a fresh run id with null parent, so appending recovered work made the combined chain unverifiable. | yes | `5f51b0d5` — a checkpointer adopts an existing VERIFIABLE chain (run identity, parent quadruple, continued iteration count); `resume=False` opts out. |
| SYS-05 | High | Promotion/serving | An explicitly supplied missing or misspelled alias silently fell back to the latest unpromoted version, bypassing promotion and rollback. | yes | `6cb4f525` — a named alias must exist or `KeyError`; the unnamed bootstrap path is preserved. |
| SYS-06 | Medium | Failed-install rollback | A CRC-corrupt wheel left unregistered package files in an empty venv, importable as `0+unknown`. | yes | `a7820684` — a partial install is reported as such with its orphaned-file count and remedy; the post-publish install step destroys a failed environment. |
| SYS-07 | Medium | Production provenance | Installed-wheel `environment_info()` ignored valid embedded build provenance, reporting git commit null/unknown. | yes | `7ad78324` — falls back to the artifact's embedded provenance; `provenance_source` names which answered. |
| SYS-08 | Medium | Source-content identity | `source_content_sha256` was computed over the reduced sdist universe while the same algorithm over the frozen checkout yields a different digest. | yes | `a2ea429e` — the record states `source_content_file_count` and `source_content_universe` alongside the value. |

Advisory (not a product finding): the declared numpy floor 2.4.0 is yanked upstream. Raised to
2.4.1 in `pyproject.toml` and `constraints-min.txt` (`7ad78324`, `7229d576`).

The acceptance is met only when every supported/optional-CI backend, both release
artifacts, every supported Python/OS tier available to the reviewer, every release-current
benchmark, checkpoint interruption/recovery, corrupted/truncated artifact rejection, and
rollback behavior have a pass or an explicit claim-removal disposition. High-severity
findings must be resolved and independently retested before RC1
(the release-candidate-stages appendix of `0.8.0.md`).
