[
 {
  "id": "Q05-F01",
  "severity": "blocking",
  "title": "fit_with_provenance, Monitor.update and the scoring verbs bypass the 0.8.2 fit-verb front door: a DataFrame, a mapping of columns, a str/bytes and a 2-level MultiIndex frame are silently fitted or scored as their column names / characters, and the inputs optimize() now refuses by name still crash the old way",
  "surface": "mixle.inference.production.fit_with_provenance (provenance.py:_records + list()), Monitor.update (retrains through it), Service.score, Service.check_drift, detect_drift, Monitor.check",
  "reproduction": "import numpy as np, pandas as pd\nfrom mixle.inference import optimize\nfrom mixle.inference.production import fit_with_provenance, Service, detect_drift, Monitor\nfrom mixle.stats import CategoricalEstimator, GaussianEstimator, PoissonEstimator, CompositeEstimator\nrng = np.random.RandomState(0)\ndf = pd.DataFrame({'x': rng.normal(size=400), 'k': rng.poisson(3, 400)})\nprint(type(optimize(df, max_its=2)).__name__)                      # CompositeDistribution over 400 rows\nm, h = fit_with_provenance(df, None, max_its=2); print(m, h.n_records)   # Categorical({'k':.5,'x':.5}), n_records=2\nprint(fit_with_provenance('hello world hello', None, max_its=2)[1].n_records)  # 17 (optimize() refuses the str)\nprint(fit_with_provenance({'x': rng.normal(size=50).tolist(), 'k': rng.poisson(3, 50).tolist()}, None, max_its=2)[0])  # Categorical over the names\nlabels = rng.choice(list('abc'), 300).tolist(); cm = optimize(labels, CategoricalEstimator())\ndf2 = pd.DataFrame({'a': labels[:150], 'b': labels[150:]})\ns = Service(cm); print(s.score(df2), s.activity[-1]['n'])          # two scores, for the column NAMES 'a' and 'b'\nprint(detect_drift(cm, labels, df2).score['n_current'])            # 2\nprint(Monitor(cm, CategoricalEstimator(), labels).update(df2)['action'])  # 'retrained' -- on the two column names\ncomp = optimize([(float(a), int(b)) for a, b in zip(df['x'], df['k'])], CompositeEstimator([GaussianEstimator(), PoissonEstimator()]))\nService(comp).score(df)   # ContractError: row 0 ... got str\n# full route table: probes/p05b_lifecycle_fixed.py (section 'route parity'), probes/p18_recovery_misc.py (a), probes/p15_xver_frontdoor.py",
  "observed": "Route table (out/p05b_lifecycle_fixed_full.txt): for the same 14 inputs optimize/propose/Model().fit/Model().evaluate agree, fit_with_provenance does not -- DataFrame -> CategoricalDistribution over the 2 column names (n_records=2; optimize: HeterogeneousBayesianNetwork over 60 rows); mapping of columns and mapping of Series -> Categorical over the names (optimize: HeterogeneousBayesianNetwork); bare str -> Categorical over 17 characters, bytes -> over 11 (optimize/propose/Model().fit/evaluate: ValueError naming the verb); 2-level MultiIndex DataFrame -> CompositeDistribution with n_records=2 (optimize: CopulaDistribution over the rows); structured array -> TypeError 'dataset_hash/model_hash cannot canonically encode numpy.void'; numpy.matrix -> RecursionError; timedelta64 -> TypeError int() ... 'datetime.timedelta'; masked array -> 'GumbelDistribution requires real-valued observations' (mask silently dropped); 0-d array -> TypeError 'iteration over a 0-d array'. Scoring verbs (out/p18_recovery_misc_full.txt, out/p03_drift_service_monitor_full.txt, out/p15_xver_frontdoor_082.txt): Service(categorical).score(df2) returns [-1.079, -1.04] with activity n=2, n_unscorable=0 (the column names 'a','b' scored as observations); detect_drift(cm, labels, df2) -> drift=True, n_current=2; Monitor(...).update(df2)['action'] == 'retrained' (the production model was refitted on the two column names); on a composite model the same frame raises 'CompositeDistribution observation must be a tuple-like sequence' / ContractError 'row 0 ... got str'; fit_with_provenance(df[['x']], GaussianEstimator()) -> ValueError could not convert string to float: 'x'. Identical on 0.8.1 (out/p15_xver_frontdoor_081.txt) except that optimize('hello world hello') was also accepted there.",
  "expected": "The CHANGELOG (R06-F03) says 'One table now gets one answer whichever verb reads it' and (R06-F06) that the migration guide's refusal list covers 'a bare str/bytes into any fit verb'; the migration guide says a bare str 'raises instead of being fitted as a categorical over its individual characters'. fit_with_provenance is documented as 'Fit estimator on data via EM (optimize)' and Monitor.update retrains through it; both should read data through the same normalize_input front door (DataFrame -> records, mapping -> column records, str/bytes/matrix/masked/0-d refused by name) and Service/detect_drift/Monitor should encode a DataFrame as its rows, as optimize does, or refuse it by name -- never score or retrain on its column labels.",
  "notes": "Mechanism: provenance.py _records() returns the object unchanged unless it has .records(), and fit_with_provenance does materialized_data = list(_records(data)); list(DataFrame) is its column labels, list(mapping) its keys, list(str) its characters, so optimize() receives a list of strings and (estimator=None) fits a categorical -- the exact P06-F10 symptom the release says is fixed. drift.py/serving.py hand the batch to model.dist_to_encoder().seq_encode(batch), which iterates a DataFrame the same way. The 0.8.2 front door (estimation.normalize_input, R06-F03) was wired into optimize/fit/best_of/propose/Model but not into the production module. This is the dominant silent-wrong-answer route left in the area: the header even records n_records=2 and a data hash of the column names, so the provenance record certifies the wrong dataset.",
  "repairs_attacked": [
   "R06-F03",
   "R06-F06",
   "P06-F10",
   "P06-F06",
   "P06-F07",
   "P06-F09",
   "P06-F03"
  ]
 },
 {
  "id": "Q05-F02",
  "severity": "real",
  "title": "fit_with_provenance(print_iter=N) with N != 1: KeyError 'delta' after the whole fit (lineage=True), or a header whose iterations/converged fields describe only every Nth iteration (lineage=False); print_iter=0 records zero iterations",
  "surface": "mixle.inference.production.fit_with_provenance (provenance.py:455 setdefault('print_iter', 1); :498-503 recs[-1]['delta']); Monitor.update forwards **kw to it",
  "reproduction": "import numpy as np\nfrom mixle.inference.production import fit_with_provenance, verify_lineage\nfrom mixle.stats import GaussianEstimator, MixtureEstimator\ng = np.random.RandomState(0).normal(2, 1.5, 400).tolist()\nest = lambda: MixtureEstimator([GaussianEstimator(), GaussianEstimator()])\nfit_with_provenance(g, est(), seed=1, max_its=10, print_iter=3)                  # KeyError: 'delta'\nm, h = fit_with_provenance(g, est(), seed=1, max_its=10, print_iter=3, lineage=False)\nprint(h.training['iterations'], m.fit_provenance().iterations, len(h.training['convergence']))  # 9 10 3\nm, h = fit_with_provenance(g, est(), seed=1, max_its=10, print_iter=0, lineage=False)\nprint(h.training['iterations'], h.training.get('converged'))                     # 0 None\n# probes/p01_provenance.py (section 2), probes/p12_variations.py (A), probes/p18_recovery_misc.py (d)",
  "observed": "print_iter=3, 7 or 0 with the default lineage=True: KeyError: 'delta' raised from fit_with_provenance after optimize() has completed (out/p01_provenance_full.txt, out/p12_variations_082.txt, out/p18_recovery_misc_full.txt). With lineage=False: print_iter=3 -> header iterations=9, converged=False, 3 trace records while the model's fit_provenance().iterations is 10; print_iter=7 -> iterations=7, 1 record; print_iter=0 -> iterations=0, converged=None, empty trace, model ran 10. print_iter=1 or unset -> 10/10, verify_lineage True. Same on 0.8.1 (out/p12_variations_081.txt is identical in this section).",
  "expected": "A print cadence must not change the provenance record: every accepted iteration is recorded (or the argument is refused/ignored with a message) and the header's iterations/converged agree with the model's fit_provenance() for any print_iter; no KeyError.",
  "notes": "Mechanism: fit_with_provenance installs its _EMHistory collector as out= and only setdefault()s print_iter=1, so a caller's print_iter reaches optimize() and throttles the em_record hook (loglik/delta) to every Nth iteration, while the on_step hook (model hash chain) fires every iteration; the two are merged by iteration, so with lineage=True the last record has lineage fields but no 'delta' and recs[-1]['delta'] raises; with lineage=False only the Nth records exist and 'iterations' is read from the last one. 0.8.2 made an explicit print_iter a first-class control (P08-F04: 'an explicit value supplies sys.stdout when no out is given') without covering this route. The docstring mentions out= but not print_iter=. Not a wrong model, but the fit is lost to the KeyError or the audit record undercounts what ran.",
  "repairs_attacked": [
   "P08-F04"
  ]
 },
 {
  "id": "Q05-F03",
  "severity": "real",
  "title": "The automatic-structure route (estimator=None on tabular records) never calls on_step: Registry.checkpointer writes no checkpoint, and fit_with_provenance returns a header with lineage_status='recorded', iterations=0, an empty trace, verify_lineage False, and the caller's max_its recorded although the structure search ran its own internal fits",
  "surface": "mixle.inference.optimize/fit(data, None, on_step=...), mixle.Model().fit(tabular, on_step=...), Registry.checkpointer, fit_with_provenance(tabular, None)",
  "reproduction": "import numpy as np, tempfile\nfrom mixle.inference import optimize\nfrom mixle.inference.production import Registry, fit_with_provenance, verify_lineage\nrng = np.random.RandomState(0); d = rng.normal(0, 1, 300).tolist() + rng.normal(6, 1, 300).tolist()\ntab = [(float(x), int(abs(x)), 'ab'[i % 2]) for i, x in enumerate(d)]\ncalls = []; m = optimize(tab, None, max_its=50, delta=None, out=None, on_step=lambda s: calls.append(s.iter))\nprint(type(m).__name__, calls, m.fit_provenance().iterations)      # HeterogeneousBayesianNetwork [] 1\nreg = Registry(tempfile.mkdtemp()); optimize(tab, None, max_its=3, delta=None, out=None, on_step=reg.checkpointer('auto')); print(reg.versions('auto'))  # []\nmm, h = fit_with_provenance(tab, None, max_its=50, delta=None)\nprint(h.training['lineage_status'], h.training['iterations'], len(h.training['convergence']), h.training['max_its'], verify_lineage(h))  # recorded 0 0 50 False\ncalls = []; optimize(d, None, max_its=5, delta=None, out=None, on_step=lambda s: calls.append(s.iter)); print(calls)  # [1..5] on scalar data\n# probes/p12_variations.py (D), probes/p19_auto_route_and_081_dirichlet.py (a)",
  "observed": "optimize(tab, None, on_step=cb), fit(tab, on_step=cb), Model().fit(tab, on_step=cb) and fit_with_provenance(tab, None, on_step=cb): cb is never called; the same data with an explicit CompositeEstimator, or scalar data with estimator=None, calls it every iteration ([1,2,3,...]). Registry.checkpointer on the automatic route: versions == [] (out/p12_variations_082.txt). fit_with_provenance(tab, None, max_its=50, delta=None): header training.lineage_status='recorded', iterations=0, convergence=[], max_its=50, verify_lineage(h)=False, while the returned HeterogeneousBayesianNetwork's fit_provenance().iterations == 1 (out/p19_auto_route_and_081_dirichlet_full.txt). The 0.8.1 registry written by probes/p07_write_081.py shows the same: 'auto_tabular' chain [] and verify_lineage False (out/p07_write_081.txt, out/p07_read_082.txt).",
  "expected": "Either the structure search's winning fit runs through the caller's optimize controls (on_step/out/max_its/delta) so checkpoints and the provenance trace exist, or the automatic route states that it ignores them: the header should say lineage_status='not_recorded' (as it does for a user out=) and not record a max_its the fit did not use; the checkpointer should not silently write nothing.",
  "notes": "Mechanism: estimation.optimize takes the structure='auto' branch when estimator is None and the records are tabular (_maybe_structured_model); the winner comes back from the structure search's own internal optimize() calls (out=None, their own caps) and the caller's on_step/out never reach an EM loop. fit_with_provenance sets lineage_status from 'collector is not None and lineage', i.e. from what it INSTALLED, not from what fired, so an empty trace is labelled recorded; verify_lineage then returns False, which reads as tampering. Adjacent to P06-F05 (the automatic path and DataSources) but not ledgered: no followups entry mentions on_step or checkpoints on this route.",
  "repairs_attacked": []
 },
 {
  "id": "Q05-F04",
  "severity": "real",
  "title": "Under a fixed iteration budget the model handed to on_step at iteration k -- and so every Registry.checkpointer snapshot -- is the previous iterate: the chain tip is one EM step behind the model optimize()/Model.fit() return and carries its iteration number, while the same call with monotone=False reports the post-step models",
  "surface": "mixle.inference.optimize(on_step=...) / EMStep under the default acceptance policy with delta=None (or a max_its cap that ends the run), Registry.checkpointer(name, every=1), Model.fit(on_step=...); fit_with_provenance's convergence trace",
  "reproduction": "import numpy as np, tempfile\nfrom mixle.inference import optimize\nfrom mixle.inference.production import Registry\nfrom mixle.stats import GaussianEstimator, MixtureEstimator\nfrom mixle.data.hashing import model_hash\nrng = np.random.RandomState(0); d = rng.normal(0, 1, 300).tolist() + rng.normal(6, 1, 300).tolist()\nest = lambda: MixtureEstimator([GaussianEstimator(), GaussianEstimator()])\nlast = {}\nm = optimize(d, est(), seed=0, delta=None, max_its=4, out=None, on_step=lambda s: last.update(iter=s.iter, model=s.model, ll=s.log_density))\nprint(last['iter'], model_hash(last['model'])[:8], model_hash(m)[:8])          # 4 6d5572b0 b36689fe  -- not the same model\nprint(model_hash(optimize(d, est(), max_its=1, delta=None, out=None, prev_estimate=last['model'])) == model_hash(m))   # True: returned = last step + one EM step\nm2 = optimize(d, est(), seed=0, delta=None, max_its=4, out=None, monotone=False, on_step=lambda s: last.update(model=s.model))\nprint(model_hash(last['model']) == model_hash(m2))                                # True under monotone=False\nreg = Registry(tempfile.mkdtemp()); m3 = optimize(d, est(), seed=0, delta=None, max_its=4, out=None, on_step=reg.checkpointer('run', every=1))\nprint(reg.versions('run'), reg.verify_chain('run'), model_hash(reg.get('run')[0]) == model_hash(m3))   # ['v1'..'v4'] True False\n# probes/p22_modelfit_vs_optimize.py, p23_restart_notes.py, p24_final_objective_offbyone.py (run in venv-full, venv-nonumba, venv-base), p21_restarts_none_control.py, p20 (b), p18 (b)",
  "observed": "optimize(600 bimodal points, 2-Gaussian mixture, seed=0, delta=None, max_its=4, on_step=cb): cb receives (1, hash 01e2d46a, ll -1313.8354), (2, ec6fad8f, -1266.0318), (3, 05383aca, -1265.9010), (4, 6d5572b0, -1265.8945); the returned model hashes b36689fe with ll -1265.8941, is exactly one further EM step from the iteration-4 model (optimize(max_its=1, delta=None, prev_estimate=<iteration-4 model>) reproduces it), and is the model an early-stopping run (max_its=200, default delta) reports at ITS iteration 5. The same call with monotone=False reports (1, ec6fad8f), (2, 05383aca), (3, 6d5572b0), (4, b36689fe) and returns the iteration-4 model; a run that stops on convergence (10 iterations) reports the returned model at its last step. Registry.checkpointer(every=1) on the fixed-budget run therefore holds v1..v4 = the four stale iterates labelled checkpoint_iter 1..4, verify_chain True, and reg.get(name)[0] != the returned model -- reproduced through plain optimize and Model.fit(restarts=None/'auto'/1), identically in venv-full, venv-nonumba and venv-base (out/p24_final_objective_offbyone_full.txt, ..._venv-nonumba.txt, ..._venv-base.txt; out/p22_modelfit_vs_optimize_full.txt; out/p23_restart_notes_full.txt; out/p21_restarts_none_control_full.txt). fit_provenance().final_objective and the provenance header's final_loglik are the returned model's (correct); fit_with_provenance's trace records the stale iterate as iter 4, its terminal record binds the returned model, and verify_lineage is True (same outputs).",
  "expected": "EMStep's docstring says model 'is the current accepted model -- snapshot it to checkpoint, and resume with optimize(prev_estimate=...)': the snapshot labelled iteration k should be the model iteration k produced, on every acceptance policy, and the last snapshot of a run should be the model returned (or the checkpointer should write a final snapshot), so that reg.get(name) after a fit is the model the caller was handed and the two policies label the same models with the same iteration numbers.",
  "notes": "Mechanism (measured, not fully traced): the plain EM loop at estimation.py:1230-1257 reports the ACCEPTED model of the iteration (model = nxt, then on_step(EMStep(i + 1, model, ...))) and monotone=False runs through it; the default policy evidently runs a loop whose report precedes its M-step, so iteration 1 reports the initial model and the final M-step's product is returned unreported. Effect size per step is one EM update (4e-4 nats here, near convergence; larger early in a run); the harm is the silent mismatch between what was checkpointed and what was returned, plus the iteration label. fit_with_provenance is designed around a returned model that differs from the last step ('possibly restored-best' terminal record), which is why its lineage still verifies. Not ledgered; neither the 0.8.1 pass-05 report nor the targeted re-review tested tip == returned.",
  "repairs_attacked": []
 },
 {
  "id": "Q05-F05",
  "severity": "real",
  "title": "Registry.register and Registry.checkpointer accept models that have no readable JSON form, writing versions and checkpoint chains that get/current/Service.from_registry/verify_chain can never read, while dump_models refuses the same models up front",
  "surface": "mixle.inference.production.Registry.register / checkpointer / get / current / verify_chain / Service.from_registry; families: VonMises, WrappedNormal, WrappedCauchy, Kent, VonMisesFisher, Bingham (mixle.stats.directional), Wishart, MatrixNormal (mixle.stats.matrix), IntegerBernoulliSet (mixle.stats.sets), KnowledgeGraph, RandomDotProductGraph (mixle.stats.graphs)",
  "reproduction": "import numpy as np, tempfile\nfrom mixle.inference import optimize\nfrom mixle.inference.production import Registry, Service\nfrom mixle.stats import VonMisesEstimator, dump_models\nrng = np.random.RandomState(0); data = rng.vonmises(0, 2, 200).tolist()\nm = optimize(data, VonMisesEstimator(), max_its=5)\ntry: dump_models(m)\nexcept Exception as e: print('dump_models:', type(e).__name__)   # SerializationError, refused up front\nreg = Registry(tempfile.mkdtemp())\nprint(reg.register(m, 'm'), reg.versions('m'))                     # v1 ['v1']  -- accepted\nreg.get('m')                                                        # SerializationError: ... does not match its constructor-owned schema\noptimize(data, VonMisesEstimator(), max_its=3, delta=None, out=None, on_step=reg.checkpointer('ck', every=1)); print(reg.versions('ck'))  # ['v1','v2','v3']\nreg.verify_chain('ck')                                              # SerializationError (not False)\n# probes/p13_writeonly_registry.py, probes/p02_family_sweep.py, probes/p02b_family_sweep2.py, probes/p14_writeonly_families.py",
  "observed": "VonMises: dump_models -> SerializationError 'dump_models produced JSON that load_models cannot read back'; Registry.register -> 'v1', versions ['v1'], promote -> None; get/current/Service.from_registry -> SerializationError 'serialized state ... does not match its constructor-owned schema; define __pysp_setstate__'; checkpointer writes v1..v3 and verify_chain raises SerializationError (out/p13_writeonly_registry_082.txt). The same register-ok/get-fails pattern for WrappedNormal, WrappedCauchy, Kent, VonMisesFisher, Bingham, IntegerBernoulliSet, Wishart, MatrixNormal, KnowledgeGraph, RandomDotProductGraph (out/p02_family_sweep_full.txt, out/p02b_family_sweep2_full.txt, out/p14_writeonly_families_082.txt). DirichletMultinomial is refused at register() (its FitReceipt class is not registered) -- a clean refusal. 0.8.1 behaves the same for VonMises and additionally for Dirichlet (out/p13_writeonly_registry_081.txt); Dirichlet is repaired in 0.8.2 (P05-F17). 64 + 21 other fitted families register, load back hash-equal and round-trip bit-exactly (same outputs).",
  "expected": "register() and checkpointer() should apply the read-back guard dump_models already applies (or check the family against the serialization manifest) and refuse with the same message, so a registry never holds a version that cannot be served and a checkpoint chain that cannot be resumed; verify_chain should return False (or a registry error) rather than leak a SerializationError.",
  "notes": "Mechanism: registry.register writes to_serializable(model) into the version record without attempting from_json on it; dump_models does the read-back check (its error text says so). The directional/matrix modules being write-only is disclosed in the 0.8.0/0.8.1 CHANGELOG and P05-F17 covered to_json/dump_models/deploy (refused loudly, pickle fallback); the registry path is the surface with no refusal. The manifests/serialization_schema_manifest.json lists these classes among the constructor-validated schemas, which is what the trail's P14 probe was checking.",
  "repairs_attacked": [
   "P05-F17",
   "P05-F02"
  ]
 },
 {
  "id": "Q05-F06",
  "severity": "real",
  "title": "detect_drift, Service.check_drift, Monitor.check and Monitor.update crash with TypeError 'unhashable type' on dict-record and set-record models with the default per_feature=True",
  "surface": "mixle.inference.production.detect_drift / Service.check_drift / Monitor.check / Monitor.update on RecordDistribution (RecordEstimator, DictRecordEstimator), BernoulliSetDistribution, IndianBuffetProcessDistribution",
  "reproduction": "import numpy as np\nfrom mixle.inference import optimize\nfrom mixle.inference.production import detect_drift, Service, Monitor\nfrom mixle.stats import DictRecordEstimator, GaussianEstimator, PoissonEstimator, BernoulliSetEstimator\nrng = np.random.RandomState(0)\nrows = [{'a': float(a), 'b': int(b)} for a, b in zip(rng.normal(size=400), rng.poisson(3, 400))]\nest = DictRecordEstimator({'a': GaussianEstimator(), 'b': PoissonEstimator()}); rm = optimize(rows, est)\nprint(Service(rm).score(rows[200:])[:2])                                   # works\nprint(detect_drift(rm, rows[:200], rows[200:], per_feature=False).drift)  # works\ndetect_drift(rm, rows[:200], rows[200:])                                  # TypeError: unhashable type: 'dict'\nMonitor(rm, est, rows[:200]).check(rows[200:])                            # TypeError: unhashable type: 'dict'\nsrows = [set(rng.choice(list('abcde'), 2, replace=False).tolist()) for _ in range(100)]; sm = optimize(srows, BernoulliSetEstimator())\ndetect_drift(sm, srows[:50], srows[50:])                                  # TypeError: unhashable type: 'set'\n# probes/p03_drift_service_monitor.py (B), probes/p15_xver_frontdoor.py, probes/p02_family_sweep.py",
  "observed": "RecordEstimator and DictRecordEstimator models: detect_drift(dict rows) -> TypeError unhashable type: 'dict'; Monitor.check/update -> same; detect_drift(..., per_feature=False) -> drift=False, ks=0.065; Service.score(dict rows) -> [-2.826, -4.85] (out/p03_drift_service_monitor_full.txt). BernoulliSet and IndianBuffetProcess models: TypeError unhashable type: 'set' (out/p02_family_sweep_full.txt, out/p02b_family_sweep2_full.txt, out/p15_xver_frontdoor_082.txt). Identical on 0.8.1 (out/p15_xver_frontdoor_081.txt).",
  "expected": "Per-feature PSI on record models should read fields by key (and set models by membership) or per_feature should degrade to the score-only report with a note; a default-argument crash on the record types the model itself was fitted from is not acceptable.",
  "notes": "Mechanism: the per-feature pass hashes each observation (or its fields) to bin it; a dict/set row is unhashable. P05-F06 repaired ndarray batches on this same path; dict and set records were not exercised by that repair or its test. Not ledgered.",
  "repairs_attacked": []
 },
 {
  "id": "Q05-F07",
  "severity": "real",
  "title": "Monitor.update still ends a monitoring loop on an unscorable row: check() now counts it (P05-F12) but the retrain that update() runs when drift is flagged refuses the same batch, with a message naming arguments update() does not take",
  "surface": "mixle.inference.production.Monitor.update (retrain via fit_with_provenance/optimize) on Gaussian (NaN, inf, None), Exponential (negative), and any family with a support refusal",
  "reproduction": "import numpy as np\nfrom mixle.inference import optimize\nfrom mixle.inference.production import Monitor, detect_drift\nfrom mixle.stats import GaussianEstimator, ExponentialEstimator\nrng = np.random.RandomState(0); d = rng.normal(3, 1, 300).tolist(); gm = optimize(d, GaussianEstimator())\nref = d[:200]; batch = ref[:20] + [float('nan')]\nprint(detect_drift(gm, ref, batch).drift, Monitor(gm, GaussianEstimator(), ref).check(batch).drift)   # True True (counted as unscorable)\nMonitor(gm, GaussianEstimator(), ref).update(batch)   # ValueError: GaussianDistribution observations contain 1 NaN entry ... fit(..., missing='marginalize') in mixle.ppl ...\nex = rng.exponential(2, 300).tolist(); em = optimize(ex, ExponentialEstimator())\nMonitor(em, ExponentialEstimator(), ex).update([v + 10 for v in ex[:100]] + [-0.5])   # ValueError: ExponentialDistribution has support x >= 0 ...\n# probes/p04_ledger_replay.py (P05-F12 block), probes/p12_variations.py (E)",
  "observed": "0.8.2: detect_drift/Service.check_drift/Monitor.check on a batch with one NaN -> drift=True, fraction_unscorable_current=0.048 (repaired); Monitor.update on the same batch -> ValueError 'GaussianDistribution observations contain 1 NaN entry -- missing values. Drop the incomplete rows, or model the gaps: fit(..., missing=\"marginalize\") in mixle.ppl, or wrap the leaf with mixle.stats.marginalized()' (out/p04_ledger_replay_082full.txt). Also update([.., inf]) -> 'requires support x in (-inf,inf)', update([.., None]) -> TypeError float() ... 'NoneType', Exponential update(shifted + [-0.5]) -> ValueError support x >= 0, with and without combine_reference (out/p12_variations_082.txt). 0.8.1 raised UnscorableObservation from check() and update() alike (out/p04_ledger_replay_081.txt).",
  "expected": "The P05-F12 ledger entry names Monitor.check/update and says 'A Monitor loop dies on the first NaN in production data'; the CHANGELOG says an unscorable record is counted 'instead of ending a monitoring loop'. update() should drop the rows check() already classified as unscorable before retraining (or refuse the batch before the drift check, in its own words), and its message should not advise fit(missing=...) / marginalized(), which update() cannot pass (the R06-F05 pattern).",
  "notes": "Mechanism: Monitor.update calls check() (now tolerant) and, on drift, fit_with_provenance on reference+batch (or batch alone); the estimator's 0.8.2 support/NaN refusals then fire on the row check() counted. The repair covered the scoring half of the surface the ledger named. Rated real rather than blocking: the failure is loud and the drift verdict itself is now right; but the loop still dies where the ledger said it must not.",
  "repairs_attacked": [
   "P05-F12"
  ]
 },
 {
  "id": "Q05-F08",
  "severity": "real",
  "title": "The ask/tell BayesianOptimizer and propose_next do not disclose the surrogate ridging that mixle.doe.minimize now reports: BayesianOptimizer.best.numerical_repairs() is () on a run whose GP was ridged, and propose_next returns only the point",
  "surface": "mixle.doe.BayesianOptimizer.ask/tell/best (optimizer.py:136-150), mixle.doe.propose_next, mixle.doe.bayesopt._record_surrogate_repairs / _SURROGATE_REPAIRS",
  "reproduction": "import numpy as np, warnings\nfrom mixle.doe import BayesianOptimizer, propose_next, minimize\nfrom mixle.models.gaussian_process import GaussianProcessRegressor\nseen = []; orig = GaussianProcessRegressor._chol\ndef patched(self, k, eye):\n    before = self._jitter_applied; out = orig(self, k, eye)\n    if self._jitter_applied != before: seen.append(float(self._jitter_applied))\n    return out\nGaussianProcessRegressor._chol = patched\nobj = lambda p: float((p[0] - 1.0) ** 2 + (p[1] + 2.0) ** 2)\nbo = BayesianOptimizer([(-5.0, 5.0), (-5.0, 5.0)], seed=3)\nfor _ in range(16):\n    for x in np.atleast_2d(np.asarray(bo.ask(1), float)): bo.tell(x, obj(x))\nprint(len(seen), max(seen), bo.best.numerical_repairs(), bo.best.surrogate_repairs)   # escalations counted; () ()\nr = minimize(obj, [(-5.0, 5.0), (-5.0, 5.0)], n_init=5, n_iter=11, seed=3); print(len(r.numerical_repairs()))  # disclosed on this route\n# probes/p20_bo_askTell_and_restarts.py (a), probes/p08_gp_doe.py",
  "observed": "BayesianOptimizer([(-5,5),(-5,5)], seed=3), 16 ask(1)/tell rounds on (p0-1)^2+(p1+2)^2: the default GP escalated its jitter 63 times, largest ridge 3.99 (0.1 x the mean kernel diagonal), yet bo.best.numerical_repairs() == () and bo.best.surrogate_repairs == (); seed=7: 49 escalations, largest 3.7, () again; no warning on either. propose_next on a design with five near-duplicate rows (x[:5] + 1e-9): 19 escalations, largest 0.481, returns a bare (2,) ndarray, the _SURROGATE_REPAIRS context variable is None afterwards. Parity reference on the same objective and seed: minimize(..., n_init=5, n_iter=11, seed=3) escalated 63 times, largest 3.99, and its result's numerical_repairs() lists 3 entries starting 'covariance-ridged(3.99; beyond jitter=1e-06, to factor the GP kernel matrix)' (out/p20_bo_askTell_and_restarts_full.txt; the 12-seed minimize sweep with escalations on 11 of 12 seeds is in out/p08_gp_doe_full.txt).",
  "expected": "The migration guide says BayesOptResult.numerical_repairs() 'reports any covariance ridging the Gaussian-process surrogate needed during the run' and 'is () when' none was needed; BayesianOptimizer.best returns a BayesOptResult, so a repaired surrogate must show there too (and propose_next needs some route to the record, e.g. a return_repairs= or a result object), the same way on every route.",
  "notes": "Mechanism (bayesopt.py:417-445, 473; optimizer.py:136-150): _fit_surrogate records the GP's numerical_repairs() into the _SURROGATE_REPAIRS ContextVar, which only minimize() sets; on the ask/tell route and in a bare propose_next() the ContextVar is None and the record is dropped, and BayesianOptimizer.best constructs BayesOptResult without surrogate_repairs, so the field is its default (). R07-F05 fixed exactly this loss for minimize; the other two public routes to the same surrogate were left as they were. The R02-F14 largest-jitter reporting itself is correct on the GP (out/p08_gp_doe_full.txt: 1e-2 then 1e-10 -> reported 0.01 once).",
  "repairs_attacked": [
   "R07-F05",
   "R02-F14",
   "P09-F03"
  ]
 },
 {
  "id": "Q05-F09",
  "severity": "docs",
  "title": "Migration guide: 'mixle models and posteriors ... are unaffected and seed exactly as before' is false -- a mixle model used as a calibrated-generator prompt seeded from the shared constant on 0.8.1 and seeds from its serialized parameters on 0.8.2",
  "surface": "docs/migrations/0.8.2.md 'Seeds derived from a prompt' (lines 95-100); mixle.task.calibrated_generator._derive_seed/_seed_key",
  "reproduction": "import numpy as np, warnings\nfrom mixle.task.calibrated_generator import _derive_seed\nfrom mixle.inference import optimize\nfrom mixle.stats import GaussianEstimator\nm = optimize(np.random.RandomState(0).normal(size=100).tolist(), GaussianEstimator())\nwith warnings.catch_warnings(record=True) as w:\n    warnings.simplefilter('always'); print(_derive_seed(7, m), [str(x.message)[:60] for x in w])\n# 0.8.1 (candidate-081 venv): 1523943581 == sha256('7:None') with the 'not reproducible' warning; 0.8.2: 2173324335, no warning\n# probes/p09_calibrated_seed.py on both interpreters",
  "observed": "0.8.1: a fitted GaussianDistribution prompt -> seed 1523943581 (== the literal '7:None' digest every non-canonical prompt shared) with the reproducibility warning; 0.8.2: 2173324335 for the same model (same for a refit and a reloaded copy), no warning (out/p09_calibrated_seed_081.txt vs out/p09_calibrated_seed_full.txt). The guide's sentence says such prompts 'are unaffected and seed exactly as before'.",
  "expected": "The guide should say that models and posteriors gained a canonical key in 0.8.2 (P09-F09) and therefore seed differently from 0.8.1, where they fell into the shared-constant bug (R07-F03) -- the same 'draws recorded on 0.8.1 ... will not reproduce' caveat it already gives for custom classes.",
  "notes": "The behaviour change is intended (P09-F09 canonical model keys; R07-F03 repair); only the migration sentence is wrong. Canonical str/bytes/int/float/None/bool/list/tuple/set/dict prompts seed identically on both versions (same probe).",
  "repairs_attacked": [
   "R07-F03",
   "P09-F09"
  ]
 },
 {
  "id": "Q05-F10",
  "severity": "minor",
  "title": "Calibrated-generator seed key of a mixle model includes its fit receipt, so parameter-identical models (equal model_hash) seed differently with no warning; numpy integer scalars are non-canonical (np.int64(3) != 3) and are warned as 'not reproducible across processes' although their repr is deterministic",
  "surface": "mixle.task.calibrated_generator._seed_key / _derive_seed",
  "reproduction": "import numpy as np, warnings\nfrom mixle.task.calibrated_generator import _derive_seed, _seed_key\nfrom mixle.inference import optimize\nfrom mixle.stats import GaussianEstimator, GaussianDistribution\nfrom mixle.data.hashing import model_hash\nd = np.random.RandomState(0).normal(size=100).tolist()\nm1 = optimize(d, GaussianEstimator()); m2 = optimize(d, GaussianEstimator(), max_its=1); m3 = GaussianDistribution(m1.mu, m1.sigma2)\nprint(model_hash(m1) == model_hash(m2) == model_hash(m3), (m1.mu, m1.sigma2) == (m2.mu, m2.sigma2))   # True True\nprint(_derive_seed(7, m1), _derive_seed(7, m2), _derive_seed(7, m3))                                 # three different seeds, no warning\nwith warnings.catch_warnings(record=True) as w:\n    warnings.simplefilter('always'); print(_derive_seed(7, np.int64(3)), _derive_seed(7, 3), _seed_key(np.int64(3)), _seed_key(3), [str(x.message)[:70] for x in w])\n# probes/p13_writeonly_registry.py (d), probes/p09_calibrated_seed.py",
  "observed": "model_hash equal for m1/m2/m3 and parameters equal, seeds 814817323 / 1586366294 / 888214437, warnings: 0 (out/p13_writeonly_registry_082.txt); _seed_key(m1) starts with the serialized object including 'fit_provenance' (out/p09_calibrated_seed_full.txt). np.int64(3) -> seed 600160120 vs 229914339 for 3 and np.float64(3.0), _seed_key(np.int64(3)) is None, and the 'seed derivation for a int64 prompt is not reproducible across processes' warning fires (same outputs).",
  "expected": "The CHANGELOG (P09-F09) says seed derivation 'recognizes a mixle model by its serialized parameters'; the key should be the parameters the library's own model_hash uses (which excludes the fit receipt), so equal models seed equally; numpy scalars should map to the canonical number they represent, and a prompt whose repr IS stable should not be warned as irreproducible.",
  "notes": "Mechanism: _seed_key encodes a model through the full serialized form, which carries fit_provenance (iterations, objective, seed); model_hash excludes that block. numpy scalars hit no branch (only ndarray and Python numbers are canonical) and fall to repr with the warning. Reproducible across processes as long as the fit is identical, hence minor.",
  "repairs_attacked": [
   "P09-F09"
  ]
 },
 {
  "id": "Q05-F11",
  "severity": "docs",
  "title": "The content hash of the families re-serialized in 0.8.2 changed between 0.8.1 and 0.8.2 (Dirichlet measured): a 0.8.1 deploy artifact loads with an integrity note claiming a content-hash mismatch, and a 0.8.1 header's model_hash no longer matches the model; the migration guide's 'every 0.8.1 pickle, JSON model and checkpoint loads unchanged' does not disclose it",
  "surface": "docs/migrations/0.8.2.md line 5-6; mixle.data.hashing.model_hash; Model.load integrity note; Registry P05-F02 cross-check; families: Dirichlet (measured), DirichletProcessMixture, MultivariateStudentT, ProbabilisticPCA, HierarchicalMixture, JointMixture, GaussianCopula, RVineCopula (same re-serialization, not measured)",
  "reproduction": "# write on 0.8.1:  REVIEW_ROOT/tools/pyt.py 600 <review-store>/candidate-081/venv/bin/python probes/p07_write_081.py   (PYT_CWD=<workdir>)\n# read on 0.8.2:   probes/p19_auto_route_and_081_dirichlet.py (b)\nimport json, pickle\nfrom mixle.lifecycle import Model\nfrom mixle.data.hashing import model_hash\nX = 'REVIEW_ROOT/pass-05/xver'\nm = Model.load(X + '/deploy_dirichlet', trust_code=True); print(m.notes)\npk = pickle.load(open(X + '/dirichlet.pkl', 'rb')); print(model_hash(pk), json.load(open(X + '/summary_081.json'))['dirichlet']['model_hash'])",
  "observed": "Model.load of the 0.8.1 Dirichlet artifact on 0.8.2: note 'integrity note: the loaded model's content hash (9cc291be62561230...) does not match the manifest's model_content_hash (81adf347369f3b01...); ... (a swapped model file, or a content-hash change between mixle versions)' plus a UserWarning; 0.8.1-recorded model_hash 81adf347... vs 0.8.2 model_hash of the unpickled model 9cc291be...; the 0.8.1 header's model_hash != model_hash(model) on 0.8.2; Gaussian's hash is identical across versions (out/p19_auto_route_and_081_dirichlet_full.txt). 0.8.1 registry records for Dirichlet/VonMises are unreadable on 0.8.2 ('serialized Dirichlet state does not match its constructor parameters (extra=[alpha_ma, dim, fit_metadata, has_invalid, log_const])') -- and were already unreadable on 0.8.1 itself (out/p13_writeonly_registry_081.txt), so that part is not a regression. Every other 0.8.1 registry record, checkpoint chain, deploy artifact, dump_models text and pickle written by probes/p07_write_081.py loads on 0.8.2 hash-equal (out/p07_read_082.txt).",
  "expected": "The migration guide should state that the eight re-serialized families hash differently from 0.8.1 (their serialized state is now the constructor parameters), so 0.8.1 headers, manifests and any stored model_hash for them will not match on 0.8.2 and the Model.load integrity note is expected, not tampering.",
  "notes": "Consequence of P05-F17/P09-F05/P10-F05 (constructor-parameter serialization changes the canonical bytes model_hash digests). The Model.load note already anticipates 'a content-hash change between mixle versions'; the guide does not.",
  "repairs_attacked": [
   "P05-F17",
   "P09-F05",
   "P10-F05"
  ]
 },
 {
  "id": "Q05-F12",
  "severity": "docs",
  "title": "experimental_designs notebook: the Halton design is the only unseeded call in its comparison cell, so the printed min-distance is irreproducible; the stored 0.048 is seed 0's draw, the executed copy printed 0.08, a further run 0.101",
  "surface": "corpus notebooks/data_science/experimental_designs.ipynb cell 1 (halton_design(bounds, 24) without seed=); mixle.doe.halton_design(seed=None, scramble=True)",
  "reproduction": "import numpy as np\nfrom mixle.doe import halton_design\nfrom scipy.spatial.distance import pdist\nb = [(0, 1), (0, 1)]\nprint([round(float(pdist(halton_design(b, 24)).min()), 3) for _ in range(3)])   # differs run to run\nprint(round(float(pdist(halton_design(b, 24, seed=0)).min()), 3))                # 0.048 == the stored output\n# probes/p17_halton_xver.py on 0.8.1 and 0.8.2; out/p16_nb_compare_rest.txt has the stored vs fresh cell text",
  "observed": "Stored cell text: {'random': 0.027, 'Latin hypercube': 0.043, 'maximin LHS': 0.091, 'Sobol': 0.065, 'Halton': 0.048}; executed copy: ... 'Halton': 0.08 (the four seeded designs identical); a fresh unseeded call gives 0.101 on both 0.8.1 and 0.8.2 with different point sets, seed=0 gives 0.048, seed=1 0.097, scramble=False 0.119 (out/p17_halton_xver_082.txt, out/p17_halton_xver_081.txt, out/p11_nb_compare.txt, out/p16_nb_compare_rest.txt).",
  "expected": "The notebook should pass seed= to halton_design like it does to the other four designs, so the stored number and the ranking sentence ('larger min-distance = better space-filling') are reproducible; no library change is implied (halton_design(seed=None) is documented as randomized).",
  "notes": "Not a library defect: halton_design's scramble is seeded by the caller and the notebook does not seed it. The prose makes no numeric claim about Halton beyond the printed dict.",
  "repairs_attacked": []
 },
 {
  "id": "Q05-F13",
  "severity": "minor",
  "title": "numpy scalar spellings are refused inconsistently across the production verbs: seed=np.int64(5) is rejected by fit_with_provenance and Monitor.update (optimize accepts it), Monitor.update(retrain=np.bool_(True)) and checkpointer(resume=np.True_) are rejected, and Registry.register(metadata={'x': np.int64(1)}) fails with the raw 'Object of type int64 is not JSON serializable'",
  "surface": "mixle.inference.production.fit_with_provenance(seed=), Monitor.update(retrain=, combine_reference=, seed=), Registry.checkpointer(resume=), Registry.register(metadata=)",
  "reproduction": "import numpy as np, tempfile\nfrom mixle.inference import optimize\nfrom mixle.inference.production import fit_with_provenance, Monitor, Registry\nfrom mixle.stats import GaussianEstimator\nd = np.random.RandomState(0).normal(size=300).tolist(); gm = optimize(d, GaussianEstimator())\nprint(type(optimize(d, GaussianEstimator(), seed=np.int64(5), max_its=2)).__name__)     # accepted\nfit_with_provenance(d, GaussianEstimator(), seed=np.int64(5), max_its=2)               # ValueError: seed must be a nonnegative integer or None\nMonitor(gm, GaussianEstimator(), d[:200]).update(d[200:], retrain=np.bool_(True))      # TypeError: retrain and combine_reference must be booleans\nreg = Registry(tempfile.mkdtemp()); reg.checkpointer('r', resume=np.True_)              # TypeError: resume must be True or False, got np.True_\nreg.register(gm, 'm', metadata={'x': np.int64(1)})                                     # TypeError: Object of type int64 is not JSON serializable\n# probes/p01_provenance.py (6), probes/p03_drift_service_monitor.py (F), probes/p04_ledger_replay.py (P05-F09/F13 blocks), probes/p12_variations.py (B)",
  "observed": "As in the reproduction (out/p01_provenance_full.txt, out/p03_drift_service_monitor_full.txt, out/p04_ledger_replay_082full.txt, out/p12_variations_082.txt); checkpointer(every=np.int64(2)) and Service(keep=np.int64(3)) ARE accepted, so the same release accepts numpy integers on two knobs and refuses them on the neighbouring ones. Same on 0.8.1 for seed/metadata.",
  "expected": "One rule for numpy scalars across the module: accept np.integer where an int is meant (as optimize, checkpointer(every=) and Service(keep=) do), accept np.bool_ where a bool is meant or say so, and let register() convert numpy scalars in metadata or refuse them with a registry message rather than json's.",
  "notes": "The validators added for P05-F09/P05-F10/P05-F16 admit np.integer for every= and keep= but the seed check (isinstance(seed, int)) and the bool checks (isinstance(x, bool)) do not; metadata goes to json.dumps unconverted.",
  "repairs_attacked": [
   "P05-F09",
   "P05-F13",
   "P05-F16"
  ]
 },
 {
  "id": "Q05-F14",
  "severity": "minor",
  "title": "Assorted validation gaps and unhelpful messages on the registry, header, serving and monitor surfaces: Header.from_dict on a partial dict raises from str(); Service.score/Monitor.check on a scalar say \"'float' object is not iterable\"; Service.score([.., None]) raises where [.., nan] is counted unscorable; Registry.promote accepts a version whose file fails the P05-F01 integrity check; a non-UTF-8 version file and a symlink-looped alias file still surface as raw UnicodeDecodeError / OSError(ELOOP); Monitor.suggest_samples accepts any method= string silently",
  "surface": "mixle.inference.production.Header.from_dict/__str__, Service.score, Monitor.check, Registry.promote, Monitor.suggest_samples",
  "reproduction": "import os, json, tempfile, numpy as np\nfrom mixle.inference import optimize\nfrom mixle.inference.production import Header, Service, Monitor, Registry\nfrom mixle.stats import GaussianEstimator, GaussianDistribution\nd = np.random.RandomState(0).normal(size=300).tolist(); gm = optimize(d, GaussianEstimator())\nstr(Header.from_dict({}))                          # AttributeError: 'NoneType' object has no attribute 'items'\nstr(Header.from_dict({'model_type': 'G', 'schema': [], 'training': {}}))   # TypeError: 'NoneType' object is not subscriptable\nService(gm).score(1.0)                             # TypeError: 'float' object is not iterable\ns = Service(gm); print(s.score(d[:3] + [float('nan')]), s.activity[-1]['n_unscorable'])   # [-..., -inf] 1\nService(gm).score(d[:3] + [None])                  # ValueError: ... contain 1 NaN entry -- missing values ...\nroot = tempfile.mkdtemp(); reg = Registry(root); reg.register(GaussianDistribution(1, 1), 'm'); reg.register(GaussianDistribution(9, 1), 'm')\nos.rename(os.path.join(root, 'm', 'v2.json'), os.path.join(root, 'm', 'v3.json'))\nprint(reg.promote('m', 'v3'))                      # None (accepted); reg.current('m', 'production') then raises the integrity failure\nprint(np.asarray(Monitor(gm, GaussianEstimator(), d[:5]).suggest_samples([(0, 1)] * 2, 4, method='bogus', seed=0)).shape)   # (4, 2) -- LHS, silently\n# probes/p01_provenance.py (8), probes/p03_drift_service_monitor.py (F), probes/p04_ledger_replay.py (P05-F01 block), probes/p12_variations.py (E, G)",
  "observed": "As in the reproduction (out/p01_provenance_full.txt, out/p03_drift_service_monitor_full.txt, out/p04_ledger_replay_082full.txt, out/p12_variations_082.txt). Additionally from the P05-F11 replay (out/p04_ledger_replay_082full.txt): a version file overwritten with bytes b'\\xff\\xfe\\x00garbage' -> reg.get raises UnicodeDecodeError \"'utf-8' codec can't decode byte 0xff in position 0\" (raw), and a symlink loop at m/production.alias -> reg.current('m', 'production') raises OSError [Errno 62] Too many levels of symbolic links (raw), whereas the same loop at m/v5.json is wrapped as 'registry ... could not be read as a registry record'. Registry-returned headers round-trip through Header.from_dict/str fine; only partial dicts break.",
  "expected": "Header.from_dict should default the missing blocks (or refuse a non-header dict by name); Service.score/Monitor.check should refuse a scalar the way optimize refuses a 0-d array; a None row should be counted like NaN (it becomes NaN one frame later) or refused by row index; promote should read the record it points at (the P05-F01 check) so an alias can never target an inconsistent file; every damaged or foreign file, including a non-UTF-8 one and a looped alias, should get the registry message P05-F11 promises; suggest_samples should refuse an unknown method (its docstring lists 'lhs'/'sobol').",
  "notes": "suggest_samples (monitor.py:102-115) tests method == 'sobol' and falls through to LHS for anything else. promote writes the alias without reading the version file. The None-row difference comes from Service.score's unscorable pre-scan seeing a float NaN but not a None that only becomes NaN inside the encoder. The P05-F11 wrapper covers JSON decoding and the version-file read but not the text decoding step nor the alias-file read.",
  "repairs_attacked": [
   "P05-F01",
   "P05-F11",
   "P05-F12",
   "P05-F16"
  ]
 },
 {
  "id": "Q05-F15",
  "severity": "minor",
  "title": "mixle.doe.minimize(seed=np.random.default_rng(3)) and seed='3' fail with numpy's cast error instead of a message naming seed=; GaussianProcessRegressor(jitter=True) is accepted as jitter=1.0",
  "surface": "mixle.doe.minimize(seed=), mixle.models.gaussian_process.GaussianProcessRegressor(jitter=, noise=)",
  "reproduction": "import numpy as np\nfrom mixle.doe import minimize\nfrom mixle.models.gaussian_process import GaussianProcessRegressor\nobj = lambda p: float((p[0] - 1.0) ** 2 + (p[1] + 2.0) ** 2)\nminimize(obj, [(-5, 5), (-5, 5)], n_init=4, n_iter=3, seed=np.random.default_rng(3))   # TypeError: Cannot cast scalar from dtype('O') to dtype('int64') according to the rule 'safe'\nminimize(obj, [(-5, 5), (-5, 5)], n_init=4, n_iter=3, seed='3')                        # TypeError: Cannot cast scalar from dtype('<U1') to dtype('int64') ...\nprint(GaussianProcessRegressor(lengthscale=1.0, amplitude=1.0, jitter=True).jitter)     # 1.0\n# probes/p08_gp_doe.py",
  "observed": "As in the reproduction (out/p08_gp_doe_full.txt); seed=np.int64(3) and an int work; jitter=0/-1/nan and noise=0/-1 are refused by name.",
  "expected": "seed= should be validated like the fit verbs do (int, RandomState, or refused by name; a Generator either accepted or named); a bool jitter should be refused like a bool print_iter is.",
  "notes": "Requires torch (venv-full); in venv-base minimize/propose_next refuse before any evaluation with the documented torch message (out/p08_gp_doe_base.txt).",
  "repairs_attacked": []
 },
 {
  "id": "Q05-F16",
  "severity": "minor",
  "title": "The lifecycle Model wrapper handed to the production verbs gets three different wrong-shaped refusals and one silent acceptance: build_header(Model, data) returns a header for model_type 'Model' with model_hash None; Registry.register/dump_models/to_json/model_hash say \"callable Model(...) is not registered; use register_serializable_callable()\"; Service/detect_drift/Monitor raise AttributeError 'dist_to_encoder'/'log_density'",
  "surface": "mixle.inference.production.build_header / Registry.register / Service / detect_drift / Monitor; mixle.stats.dump_models; mixle.data.hashing.model_hash with a mixle.Model instead of model.fitted",
  "reproduction": "import numpy as np, tempfile, mixle\nfrom mixle.inference.production import build_header, Registry, Service, detect_drift\nfrom mixle.stats import GaussianEstimator, dump_models\nfrom mixle.data.hashing import model_hash\nd = np.random.RandomState(0).normal(size=300).tolist(); wm = mixle.Model(GaussianEstimator()).fit(d)\nh = build_header(wm, d); print(h.model_type, h.model_hash)     # Model None  (silently)\nRegistry(tempfile.mkdtemp()).register(wm, 'w')                 # SerializationError: callable Model(GaussianDistribution, fitted=True) is not registered; use register_serializable_callable()\nService(wm).score(d[:3])                                        # AttributeError: 'Model' object has no attribute 'dist_to_encoder'\ndetect_drift(wm, d[:100], d[100:])                              # AttributeError: 'Model' object has no attribute 'log_density'\n# probes/p12_variations.py (F), probes/p14_writeonly_families.py (C), probes/p05b_lifecycle_fixed.py",
  "observed": "As in the reproduction (out/p12_variations_082.txt, out/p14_writeonly_families_082.txt, out/p05b_lifecycle_fixed_full.txt). pickle of the wrapper works; Model.deploy/load is the documented path for the wrapper and works.",
  "expected": "One refusal naming the fix ('pass model.fitted, the fitted distribution, not the Model wrapper') on every production verb, and build_header should refuse a non-distribution rather than certify 'Model' with a None hash.",
  "notes": "The registry/hasher message comes from the serializer treating a callable object as an unregistered callable; the wrapper is callable and not a distribution. The trail's P14 (C) probe was written to catalogue exactly these messages.",
  "repairs_attacked": []
 },
 {
  "id": "Q05-F17",
  "severity": "minor",
  "title": "Model.fit(restarts=N) runs its restart candidates without the caller's on_step/out, so a Registry.checkpointer records only the initial fit while a kept candidate is returned; notes say 'best-of-3 restart kept' but nothing says the checkpoints predate the replacement",
  "surface": "mixle.Model.fit(restarts=<int>, on_step=...) -> lifecycle.py:_refit_symmetry_broken (optimize(..., out=None) / best_of(..., out=None) without on_step)",
  "reproduction": "import numpy as np, tempfile, warnings, mixle\nfrom mixle.inference.production import Registry\nfrom mixle.stats import GaussianEstimator, MixtureEstimator\nrng = np.random.RandomState(0); d = rng.normal(0, 1, 300).tolist() + rng.normal(6, 1, 300).tolist()\nest = lambda: MixtureEstimator([GaussianEstimator(), GaussianEstimator()])\ndef params(m): return [(round(float(c.mu), 4), round(float(c.sigma2), 4)) for c in m.components]\nwith warnings.catch_warnings():\n    warnings.simplefilter('ignore')\n    a = mixle.Model(est()).fit(d, restarts=1, max_its=4, delta=None, seed=0)\n    calls = []; reg = Registry(tempfile.mkdtemp()); ck = reg.checkpointer('mf', every=1)\n    b = mixle.Model(est()).fit(d, restarts=3, max_its=4, delta=None, seed=0, on_step=lambda s: (calls.append(s.iter), ck(s)))\nprint(a.notes, params(a.fitted)); print(b.notes, params(b.fitted), calls, reg.versions('mf'))\n# probes/p23_restart_notes.py, p20_bo_askTell_and_restarts.py (b), p18_recovery_misc.py (b)",
  "observed": "restarts=3, max_its=4, delta=None, seed=0: on_step fires four times (the initial fit's iterations 1..4), the registry holds v1..v4, verify_chain True; the returned model is [(0.0388, 1.0346), (5.8714, 0.9802)] with notes ['restarts requested: best-of-3 restart kept (log-lik +0.000)'], whereas restarts=1 (candidate not kept) returns [(0.0385, 1.0339), (5.8711, 0.9808)] with the same four on_step calls (out/p23_restart_notes_full.txt, out/p20_bo_askTell_and_restarts_full.txt). The kept candidate's trajectory never reached on_step or the checkpointer.",
  "expected": "Either forward on_step/out to the restart candidates (the winner's trajectory becoming the chain), or append to notes that the on_step/checkpoint record covers the initial fit only, so a user reading the chain knows the served model came from elsewhere.",
  "notes": "Mechanism (lifecycle.py:392-412, 440-500): after the first optimize() (with the caller's optimize_kw), _refit_symmetry_broken runs hard-partition trials and best_of() through optimize(..., out=None) with no on_step and replaces self.fitted when a candidate beats the initial fit by > 1e-6. The replacement itself is disclosed (notes) and Model.fit's docstring says restarts record 'what happened in notes'; the gap is only that the callback-side record is silently partial. Stacks with Q05-F04 (which already makes the chain tip differ from the returned model even without a kept restart).",
  "repairs_attacked": []
 }
]