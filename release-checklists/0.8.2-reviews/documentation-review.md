# Documentation stale-content review — 0.8.2

- **Date:** 2026-09-08, on the 0.8.2 candidate tree.
- **Reviewer:** release owner's agent. Machine checks run from `/tmp` against the candidate venv
  holding the 0.8.2 wheel built from this tree; prose checked against the tree itself.
- **Scope:** `docs/` — 882 `.rst`/`.md` files, of which 883 pages are written by the strict build
  (the api/ tree is generated by apidoc).

## What was checked

| Check | Result |
|---|---|
| Strict Sphinx build (`sphinx -W -b html docs`) | **exit 0, zero warnings**, 883 pages written |
| Version references naming a *previous* release as current | none: `v0.8.[01]` appears **0** times across `docs/**/*.rst` and `**/*.md` |
| The release-facing pages name 0.8.2 | `releases.md` ("Version 0.8.2 is the current release target"), `release-notes.rst`, `support-policy.rst`, `migrations/README.md`, `claim-evidence-ledger.rst` |
| Countable claims | `example-execution-manifest.rst` says 57 examples; `ls examples/*.py` = **57** |
| Historical narratives are labelled as such | `example-execution-manifest.rst` opens by stating the dated passes are "historical engineering context, **not** 0.8.2 release evidence"; `docs/audits/` are frozen finding ledgers |

## Stale items found and repaired (2)

Both in `claim-evidence-ledger.rst`, and both of the same kind: evidence bound to the *previous*
candidate, which a reader would take as evidence for this one.

1. The `"15,000+ tests"` row cited "Measured 2026-09-07: 16,538 collected ... on the 0.8.1 candidate
   `c9c5fbbb`", and closed with "that remains a 0.8.1 exit criterion". Re-measured on this tree:
   **16,849** collected by `pytest --collect-only -m ""`. The row now cites that measurement and
   this release, and states the wheel-run criterion as still open for this release rather than for
   0.8.1.
2. The preamble said several grades were "pending the 0.8.1 re-run". Reworded to "pending the
   exact-tip re-run for this release" -- the 0.8.1 re-run has happened, and what those grades are
   actually waiting on is this candidate's hosted matrix.

## Nothing else found stale

The version strings, the docs-site prefix, and the release-facing framing were all moved to 0.8.2
when the branch was cut, so this review had far less to repair than 0.8.1's (which found 31 items
across 16 pages). The remaining `0.8.0`/`0.8.1` mentions outside `api/` are release-history entries,
migration notes for those versions, deprecation windows that legitimately name the release that
announced them, and the frozen audit ledgers.
