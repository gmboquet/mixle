# mixle 0.7.0 release checklist

**Status: not started.** This release was renamed from the in-progress `0.6.3` workstream (never
published under that version) to `0.7.0`; nothing here carries over — every gate below is reset to
`TODO` and must be re-verified against this branch's tip before it counts as evidence.

- Release branch: `release/0.7.0`
- Checklist opened: 2026-07-09
- Tracking commit as of this update: (set on first verification pass)

See [`README.md`](README.md) for the status legend and evidence discipline this file follows.

## 1. Branch and CI state

| Gate | Status | Evidence |
| --- | --- | --- |
| No open PRs against the release branch | `TODO` | |
| `main` is not ahead of the release branch | `TODO` | |
| `v0.7.0` tag does not already exist | `TODO` | |
| CI is green on the exact current tip | `TODO` | |

## 2. Version and metadata

| Gate | Status | Evidence |
| --- | --- | --- |
| `pyproject.toml` version equals the release version | `TODO` | |
| `__version__` matches package metadata | `TODO` | |
| Bump is the correct semver level | `TODO` | |
| `requires-python` and classifiers are current | `TODO` | |
| `CHANGELOG.md` has a dated, complete `0.7.0` entry | `TODO` | |
| Migration notes for any breaking change | `TODO` | |

## 3. Build and install clean

| Gate | Status | Evidence |
| --- | --- | --- |
| `python -m build` produces sdist + wheel with zero errors | `TODO` | |
| `twine check dist/*` | `TODO` | |
| Fresh, isolated venv install (not editable, not `PYTHONPATH`) | `TODO` | |
| Import smoke from that install | `TODO` | |
| Full public-module import sweep | `TODO` | |
| Non-`.py` package data present in the wheel | `TODO` | |

## 4. Dependency correctness

| Gate | Status | Evidence |
| --- | --- | --- |
| Declared deps match imports (both directions) | `TODO` | |
| Dependency graph resolves clean | `TODO` | |
| Version bounds are real, not just present | `TODO` | |
| Optional deps stay truly optional | `TODO` | |
| Platform-incompatible dependency spec | `TODO` | |

## 5. Test rigor

| Gate | Status | Evidence |
| --- | --- | --- |
| `lint` (ruff format --check + ruff check; mypy advisory) | `TODO` | |
| `fast` × py3.10/3.11/3.12 | `TODO` | |
| `full` (non-optional suite, py3.12) | `TODO` | |
| `optional` (torch/numba/jax/ray/lightning extras) | `TODO` | |
| `security` / `pip-audit` (base install) | `TODO` | |
| No known flaky tests in the gate | `TODO` | |
| Supported OS matrix is stated and covered | `TODO` | |

## 6. Hygiene scan

| Gate | Status | Evidence |
| --- | --- | --- |
| No secrets in code/tests/docs | `TODO` | |
| No debug leftovers (`breakpoint()`, stray `print()`, `pdb`) | `TODO` | |
| Author/committer identities are acceptable | `TODO` | |

## 7. Documentation

| Gate | Status | Evidence |
| --- | --- | --- |
| `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md` present and current | `TODO` | |
| Sphinx docs build strict (`-W --keep-going`) | `TODO` | |
| Generated `docs/api/*.rst` current | `TODO` | |
| README reflects current version/capabilities | `TODO` | |
| Process references resolve within this repo | `TODO` | |
| Non-API doc pages' embedded code examples actually work | `TODO` | |

## 8. Examples and notebooks

| Gate | Status | Evidence |
| --- | --- | --- |
| Every shipped example executes from a clean install | `TODO` | |
| Every shipped notebook executes top-to-bottom on a clean kernel | `TODO` | |

## 9. Post-merge and final re-verification

Passing on each contributing branch is not the same as passing on the merged tip — a merge is where
cross-branch interactions surface (see [`lessons-learned.md`](lessons-learned.md)). Re-run the
confidence gates against the exact commit that will be tagged.

| Gate | Status | Evidence |
| --- | --- | --- |
| Full suite green against the exact tag commit (not just per-branch CI) | `TODO` | |
| Re-run after every commit that lands during verification | `TODO` | |

## 10. Reproducibility

A published version must stay installable and runnable years later; that depends on bounded deps plus
a captured resolution (see [`lessons-learned.md`](lessons-learned.md)).

| Gate | Status | Evidence |
| --- | --- | --- |
| Resolved dependency set captured for this release | `TODO` | |
| The *previous* release still installs and smoke-runs | `TODO` | |

## 11. Publication

Do not start until §1–§10 are `DONE` or explicitly `EXCLUDED`. Publish in dependency order — as part
of a coordinated family release, `mixle` (core) publishes before any sibling that depends on it;
that cross-package ordering is tracked by the release owner outside this repo (see the Scope note in
[`README.md`](README.md)). The steps below are `mixle`'s own.

| Step | Status | Evidence |
| --- | --- | --- |
| Build from the frozen commit: `python -m build`; `twine check dist/*` | `TODO` | |
| TestPyPI dry run: `twine upload --repository testpypi dist/*`, then install back from TestPyPI in a clean env and smoke-run | `TODO` | |
| PyPI publish: `twine upload dist/*` | `TODO` | |
| Tag the exact published commit: `git tag -a v0.7.0 -m "mixle v0.7.0"` && `git push origin v0.7.0` (never `--force`) | `TODO` | |
| Post-publish verification from the real index | `TODO` | |
| Docs / changelog / website updated to the published version | `TODO` | |

## 12. Rollback / incident readiness

A published version is immutable — you never fix a bad release in place.

| Gate | Status | Evidence |
| --- | --- | --- |
| The rollback path is understood before publishing | `TODO` | A broken `0.7.0` is fixed by publishing `0.7.1`, not by re-uploading `0.7.0` (PyPI forbids it). *Yank* only for a security/critical defect — a yank leaves it installable by exact pin so existing lockfiles keep working; it is not a delete. Confirm the owner knows which applies before cutting. |

## 13. Sign-off

| Gate | Status | Evidence |
| --- | --- | --- |
| Release owner sign-off | `TODO` | Name + date, recorded here, only after every gate above is `DONE` or `EXCLUDED`. This line is the release decision. |
