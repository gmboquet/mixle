# mixle 0.7.0 release checklist

**Status: ready to publish pending sign-off.** §1–§10 are fully `DONE`. §7's doc code-example sweep
found 4 genuine bugs, fixed in `fda357dd`; CI re-run against that exact tip (Tests run
[29038902419](https://github.com/gmboquet/mixle/actions/runs/29038902419), Security run
[29038903945](https://github.com/gmboquet/mixle/actions/runs/29038903945)) is green — §1/§5/§9's
evidence below now cites this run, not the stale `82904e61`/`29032823000` one. §8's notebook item is
`EXCLUDED` (it referred to a notebook that was never actually shipped — see the gate's evidence);
the example-execution gate is `DONE`. §11 (publication) is explicitly out of scope for this pass and
was not started. §12 is a documented understanding, no action needed. §13 (sign-off) is for the
release owner.

- Release branch: `release/0.7.0`
- Checklist opened: 2026-07-09
- Tracking commit as of this update: `718a4af5` ("release: §7 doc-sweep DONE with real findings,
  retrigger CI at fda357dd"), updated 2026-07-10

See [`README.md`](README.md) for the status legend and evidence discipline this file follows.

## 1. Branch and CI state

| Gate | Status | Evidence |
| --- | --- | --- |
| No open PRs against the release branch | `DONE` | `gh pr list --repo gmboquet/mixle --state open` → `[]`, checked 2026-07-10. |
| `main` is not ahead of the release branch | `DONE` | `git rev-list --count origin/release/0.7.0..origin/main` → `0`, checked 2026-07-10. |
| `v0.7.0` tag does not already exist | `DONE` | `git ls-remote --tags origin` has no `v0.7.0` (latest is `v0.6.2`), checked 2026-07-10. |
| CI is green on the exact current tip | `DONE` | Manually dispatched (`gh workflow run Tests --ref release/0.7.0`) directly against `fda357dd` (the §7 doc-fix commit, current tip's parent) — run [29038902419](https://github.com/gmboquet/mixle/actions/runs/29038902419), all 6 jobs `success`: `fast / py3.10`, `fast / py3.11`, `fast / py3.12`, `full / py3.12`, `lint`, `optional extras / py3.12`. Supersedes the prior run against `82904e61` (29032823000), which predated the §7 doc fixes. Checked 2026-07-10. |

## 2. Version and metadata

| Gate | Status | Evidence |
| --- | --- | --- |
| `pyproject.toml` version equals the release version | `DONE` | `version = "0.7.0"`, `grep -n '^version' pyproject.toml` confirmed 2026-07-10. |
| `__version__` matches package metadata | `DONE` | Fresh isolated venv install of the built wheel: `python -c "import mixle; print(mixle.__version__)"` → `0.7.0`. |
| Bump is the correct semver level | `DONE` | Diffed `__all__` exports (top-level `mixle/__init__.py` plus `mixle.stats`/`mixle.ppl`/`mixle.task`/`mixle.models`) between `v0.6.2` (last published tag) and this tip: zero removed/renamed symbols in any of the five — only additions (e.g. `__version__` itself is new at the top level). Purely additive → minor bump correct. |
| `requires-python` and classifiers are current | `DONE` | `requires-python = ">=3.10"`; `Programming Language :: Python :: 3.10/3.11/3.12` and `Operating System :: POSIX :: Linux` / `:: MacOS` classifiers present, matching the CI matrix and the stated platform-support policy. |
| `CHANGELOG.md` has a dated, complete `0.7.0` entry | `DONE` | `## [0.7.0] — 2026-07-09` with Added/Fixed sections describing the release's workstream. |
| Migration notes for any breaking change | `DONE` | No breaking/removed public API found in the additive-only `__all__` diff above; no migration notes needed. |

## 3. Build and install clean

| Gate | Status | Evidence |
| --- | --- | --- |
| `python -m build` produces sdist + wheel with zero errors | `DONE` | Built cleanly from `82904e61`: `mixle-0.7.0.tar.gz` and `mixle-0.7.0-py3-none-any.whl`, zero errors/warnings. |
| `twine check dist/*` | `DONE` | Both `mixle-0.7.0-py3-none-any.whl` and `mixle-0.7.0.tar.gz` → `PASSED`. |
| Fresh, isolated venv install (not editable, not `PYTHONPATH`) | `DONE` | `python3.12 -m venv /tmp/relcheck070` (the CI-matrix Python, not whatever `python3` happened to resolve to), `pip install dist/mixle-0.7.0-py3-none-any.whl`, no extras, no editable, no `PYTHONPATH`. |
| Import smoke from that install | `DONE` | `python -c "import mixle; print(mixle.__version__)"` → `0.7.0` from the bare install. |
| Full public-module import sweep | `DONE` (found and fixed 2 bugs) | `importlib.import_module` swept over every `pkgutil.walk_packages` entry (687 modules) against the bare install. First pass: 2 failures — `mixle.inference.mcmc.nuts_numba` (`from numba import njit` at module top level, used to decorate 6 module-level functions) and `mixle.utils.parallel.mpi` (`from mpi4py import MPI` at module top level) — both unguarded, same class of bug as the 8 fixed in the 0.6.3/#274 pass, missed by that sweep because they were added afterward. Fixed in `82904e61`: added `mpi4py`/`MPI`/`HAS_MPI4PY` to the central `mixle.utils.optional_deps` shim registry (alongside `numba`/`pyspark`/`gmpy2`/`zarr`/`h5py`) with `require("mpi4py", "mpi")` guards at `mpi_out()` and `MPIEncodedData.__init__`; switched `nuts_numba.py` to the existing `optional_deps` numba shim (same pattern as `mixle/stats/latent/_hidden_markov_numba_kernels.py`). Re-verified: 687/687 modules import cleanly from a fresh bare install. Confirmed the fix doesn't regress real functionality: in the full dev env, `_kinetic` (a `nuts_numba.py` function) is a genuine `numba.core.registry.CPUDispatcher`, not a shim passthrough; existing MPI tests (30 passed) and NUTS/MCMC tests (55 passed) stay green. |
| Non-`.py` package data present in the wheel | `DONE` | `unzip -l dist/*.whl`: `mixle-0.7.0.dist-info/{LICENSE,NOTICE,METADATA,WHEEL,top_level.txt,RECORD}` present, 1452 files total. |

## 4. Dependency correctness

| Gate | Status | Evidence |
| --- | --- | --- |
| Declared deps match imports (both directions) | `DONE` | AST-based sweep of every third-party top-level import across `mixle/` (excluding tests), cross-referenced against declared extras/base deps: no undeclared third-party import found. The only apparent gaps (`Cython`, `setuptools` in `mixle/engines/build_kernels.py`) are lazy/deferred imports already guarded — confirmed that module imports cleanly from the bare install with neither installed. |
| Dependency graph resolves clean | `DONE` | `pip install --dry-run '.[all]'` → resolves clean, `Would install mixle-0.7.0`, checked 2026-07-10. |
| Version bounds are real, not just present | `DONE` | Re-spot-checked `numba`, `torch`, `mpi4py`, `numpy`, `scipy` bounds against actual API surface used; consistent with prior 0.6.3-pass audit, no drift found. |
| Optional deps stay truly optional | `DONE` | Confirmed via the bare-install 687-module import sweep in §3 (0 failures after the numba/mpi fix) and the CI `fast` lane (installs only `.[test]`, green). |
| Platform-incompatible dependency spec | `DONE` | `numba`'s `tbb>=2021.6; platform_machine == 'x86_64'` PEP 508 marker (fixed in the earlier 0.6.3 pass for arm64/Apple Silicon compatibility) confirmed still present in both the `numba` and `all` extras. |

## 5. Test rigor

| Gate | Status | Evidence |
| --- | --- | --- |
| `lint` (ruff format --check + ruff check; mypy advisory) | `DONE` | Green against `fda357dd`, run [29038902419](https://github.com/gmboquet/mixle/actions/runs/29038902419) (supersedes the earlier `82904e61`/29032823000 run). Also spot-checked locally on the 4 files touched by the §7 doc fixes: `ruff check` + `ruff format --check` both clean. |
| `fast` × py3.10/3.11/3.12 | `DONE` | All three green, same run. |
| `full` (non-optional suite, py3.12) | `DONE` | Green, same run. |
| `optional` (torch/numba/jax/ray/lightning extras) | `DONE` | Manually dispatched, `success` against `fda357dd`, same run. |
| `security` / `pip-audit` (base install) | `DONE` | Manually dispatched (`gh workflow run Security --ref release/0.7.0`), run [29038903945](https://github.com/gmboquet/mixle/actions/runs/29038903945) against `fda357dd` (supersedes the earlier `82904e61`/29034196952 run). Also ran locally against the bare-install venv: `pip-audit` → "No known vulnerabilities found". |
| No known flaky tests in the gate | `DONE` | No new flakes surfaced in this pass (full/fast/optional/lint all green in one run after the numba/mpi fix); no reruns needed. |
| Supported OS matrix is stated and covered | `DONE` | CI tests Linux x86_64 (`optional extras` job runs there too); macOS arm64 is the primary dev platform but not CI-gated; Windows untested. Stated in the README Installation section, unchanged this pass. |

## 6. Hygiene scan

| Gate | Status | Evidence |
| --- | --- | --- |
| No secrets in code/tests/docs | `DONE` | Scanned for credential-shaped strings (AWS/OpenAI key patterns, private-key headers) across `mixle/` and `docs/`; every match is a fake fixture in `security_test.py`/`flagship_triage_test.py`/`harness_test.py` exercising the secret-redaction feature — not a real secret. |
| No debug leftovers (`breakpoint()`, stray `print()`, `pdb`) | `DONE` | Scanned non-test source; no `breakpoint()`/`pdb.set_trace()` leftovers found. |
| Author/committer identities are acceptable | `DONE` | `git log --format='%an <%ae>' origin/main..origin/release/0.7.0` → only `Grant Boquet <grant.boquet@gmail.com>`, checked 2026-07-10. |

## 7. Documentation

| Gate | Status | Evidence |
| --- | --- | --- |
| `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md` present and current | `DONE` | All three exist at the repo root and are current for this tip. |
| Sphinx docs build strict (`-W --keep-going`) | `DONE` | `sphinx -b html docs docs/_build/html -W --keep-going` → `build succeeded`, zero warnings, exit 0. Run twice: once before and once after the numba/mpi fix commit (`82904e61`) — both clean. |
| Generated `docs/api/*.rst` current | `DONE` | Diffed every `mixle/**/*.py` module (excluding tests) against `docs/api/*.rst` pages: zero modules missing a page (688 modules / 689 pages). Autodoc means new functions/classes inside already-documented modules (e.g. this pass's `optional_deps.py` additions) need no new page. |
| README reflects current version/capabilities | `DONE` | Version/branch references audited and corrected for the `0.6.3` → `0.7.0` rename (prior commit); content extensively audited this session (Quickstart, Enumeration, PPL, Installation sections all re-verified against live runs). |
| Process references resolve within this repo | `DONE` | `CONTRIBUTING.md` and `CHANGELOG.md` point into `release-checklists/`, not an unreachable path; confirmed unchanged this pass. |
| Non-API doc pages' embedded code examples actually work | `DONE` | Extracted every `.. code-block:: python` from all 43 narrative `.rst` pages under `docs/` (285 blocks) and executed each with same-page cumulative context (later blocks see earlier blocks' names). 201/285 individual block-runs "failed," but triage of every failure's actual `stderr` showed all but 4 are one of two intentional, non-bug patterns this codebase's docs consistently use: (1) a placeholder variable/call representing "your data" that's never defined on the page (e.g. `losses`, `records`, a bare `x, y = ...`), matching the README's own established `records = [...]` convention; or (2) an example that is documented as deliberately raising (e.g. `stability-and-missing-data.rst`'s strict-missing-data example, explicitly commented `# raises by default`). The remaining 4 were genuine bugs, fixed in `fda357dd`: `evolution.rst` used a nonexistent `log=` kwarg on `Real`; `ppl.rst`'s `constrain()` example passed an invalid second argument and called a nonexistent `.fit()` method; `stats-structured.rst` called `RecordEstimator` with a tuple of `(name, estimator)` pairs instead of the mapping form it requires; `training-at-scale.rst` had an invalid trailing bare `...` after keyword arguments (a `SyntaxError`) and an unreachable target that no longer matched its own narrative once fixed. Re-ran the corrected examples standalone (all pass) and a full `sphinx -b html -W --keep-going` build after the fix (clean, zero warnings). |

Also landed this pass, informational (not a pre-existing gate in this template): the docs site
(`gmboquet.github.io/mixle`) now builds versioned docs via `sphinx-polyversion` (`docs/poly.py`) —
every `v*.*.*` tag with a `docs/` tree plus `main`, with a Furo sidebar version switcher reading a
site-root `switcher.json`. Verified end-to-end (mock build, a real two-tag build against a disposable
clean clone, and a local-only test commit confirming the switcher actually renders) before committing
as `df38013b`. Older tags (`v0.6.1`, `v0.6.2`) will show the sidebar without a working switcher until
a version with this feature actually ships, since their own `conf.py` can't be changed retroactively.

## 8. Examples and notebooks

| Gate | Status | Evidence |
| --- | --- | --- |
| Every shipped example executes from a clean install | `DONE` | Re-ran all 23 base-install ("Execute.") examples from `docs/example-execution-manifest.rst` against the freshly bug-fixed `mixle-0.7.0` wheel in the bare venv: 21/21 previously-passing examples still pass (`enumeration_example.py`, `extensibility_seams_example.py`, both `gallery_*` families, `heterogeneous_correctness_example.py`, `hidden_association_example.py`, `hierarchical_mixture_example.py`, `joint_mixture_example.py`, `latent_variable_models_example.py`, `lookback_hmm_example.py`, `ppl_example.py`, `semi_supervised_mixture_example.py`, `structure_learning_example.py`, `structured_hmm_example.py`, `structured_leaves_example.py`, `auto_example.py`); the 2 previously-blocked examples are still correctly blocked for the same documented reasons (`skeptic_challenge_example.py` needs `sklearn`, `win_demo_example.py` needs `torch` — neither installed in the bare venv). |
| Every shipped notebook executes top-to-bottom on a clean kernel | `EXCLUDED` | The prior pass's open item ("the frontier-family showcase notebook, B6/#269") does not refer to a real notebook. Checked: PR #269 ("examples: frontier-family showcase notebook (B6)") only ever touched `examples/frontier_family_showcase.py` and `mixle/tests/frontier_family_showcase_smoke_test.py` — a script and its smoke test, no `.ipynb`. Also searched the entire `mixle-notebooks` companion repo (all ~100 notebooks across `applications/`, `architecture_studies/`, `data_science/`, `exploration_geoscience/`, `tutorials/`): nothing named `frontier_family_showcase` or close to it exists there either. There is no notebook to execute for this gate — the prior pass's evidence was based on a title/deliverable mismatch (PR #269's title said "notebook", its actual diff shipped a script). Ran the real available evidence instead: `mixle/tests/frontier_family_showcase_smoke_test.py::FrontierFamilyShowcaseSmokeTest::test_lifecycle_receipt_at_reduced_scale` → `1 passed in 25.09s`, checked 2026-07-10. If mixle ships an actual notebook in a future release, re-open this gate for it specifically. |

## 9. Post-merge and final re-verification

Passing on each contributing branch is not the same as passing on the merged tip — a merge is where
cross-branch interactions surface (see [`lessons-learned.md`](lessons-learned.md)). Re-run the
confidence gates against the exact commit that will be tagged.

| Gate | Status | Evidence |
| --- | --- | --- |
| Full suite green against the exact tag commit (not just per-branch CI) | `DONE` | See §1/§5 — run [29038902419](https://github.com/gmboquet/mixle/actions/runs/29038902419), all 6 jobs green against `fda357dd`, the commit the current tip (`718a4af5`, a checklist-only doc change) builds on. |
| Re-run after every commit that lands during verification | `DONE` | CI has been re-dispatched after every substantive commit this pass: `78326103` (pre-numba/mpi-fix, cancelled) → `82904e61` (numba/mpi fix) → `fda357dd` (§7 doc-example fixes, current green run) — the evidence above is against the *latest* substantive commit, not a stale one. |

## 10. Reproducibility

A published version must stay installable and runnable years later; that depends on bounded deps plus
a captured resolution (see [`lessons-learned.md`](lessons-learned.md)).

| Gate | Status | Evidence |
| --- | --- | --- |
| Resolved dependency set captured for this release | `DONE` | `pip freeze` from a fresh `python3.12 -m venv` install of `mixle[all]` (the built wheel), captured to [`0.7.0-freeze-all.txt`](0.7.0-freeze-all.txt) alongside this file. |
| The *previous* release still installs and smoke-runs | `DONE` | Fresh venv, `pip install mixle==0.6.2` (the last published tag): `import mixle` succeeds and `importlib.metadata.version("mixle")` → `0.6.2`. Note: `mixle.__version__` raises `AttributeError` on 0.6.2 — that attribute did not exist as a public API yet at that release (added later), not a regression; the bare-import + metadata-version check is the correct smoke test for that specific release. |

## 11. Publication

Do not start until §1–§10 are `DONE` or explicitly `EXCLUDED`. Publish in dependency order — as part
of a coordinated family release, `mixle` (core) publishes before any sibling that depends on it;
that cross-package ordering is tracked by the release owner outside this repo (see the Scope note in
[`README.md`](README.md)). The steps below are `mixle`'s own.

| Step | Status | Evidence |
| --- | --- | --- |
| Build from the frozen commit: `python -m build`; `twine check dist/*` | `TODO` | |
| TestPyPI dry run: `twine upload --repository testpypi dist/*`, then install back from TestPyPI in a clean env and smoke-run | `TODO` | |
| PyPI publish: `twine upload dist/*` | `TODO` | |
| Tag the exact published commit: `git tag -a v0.7.0 -m "mixle v0.7.0"` && `git push origin v0.7.0` (never `--force`) | `TODO` | |
| Post-publish verification from the real index | `TODO` | |
| Docs / changelog / website updated to the published version | `TODO` | |

## 12. Rollback / incident readiness

A published version is immutable — you never fix a bad release in place.

| Gate | Status | Evidence |
| --- | --- | --- |
| The rollback path is understood before publishing | `DONE` | Documented understanding, no action taken (publication is out of scope for this pass): a broken `0.7.0` is fixed by publishing `0.7.1`, not by re-uploading `0.7.0` (PyPI forbids it). *Yank* only for a security/critical defect — a yank leaves it installable by exact pin so existing lockfiles keep working; it is not a delete. The release owner should confirm which applies before cutting. |

## 13. Sign-off

| Gate | Status | Evidence |
| --- | --- | --- |
| Release owner sign-off | `TODO` | Name + date, recorded here, only after every gate above is `DONE` or `EXCLUDED`. This line is the release decision. |
