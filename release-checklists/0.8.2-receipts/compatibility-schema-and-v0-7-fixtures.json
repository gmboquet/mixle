{
 "artifact": "mixle.release_gate_receipt/v1",
 "candidate_commit": "1bcd802636b03174ab99098aa361d754c0ed5a7b",
 "command": "python3 -c \"import json;d=json.load(open('$SC/check-runs.json'));r=[x for p in d for x in p['check_runs']];sel=[x for x in r if x['name'] in ('clean-wheel install + import sweep','extras matrix / exact candidate') and x['conclusion']!='cancelled'];print('\\n'.join('%s: %s' % (x['name'],x['conclusion']) for x in sel))\"; echo -n 'cross-version fixture tests: '; PYTHONPATH=. <venv>/bin/python -m pytest mixle/tests/cross_version_fixtures_test.py -p no:randomly -q -m '' -n0 2>&1 | tail -1",
 "date": "2026-09-09",
 "evidence_digest": "7ff3f581759ead9c61bea7626ff6e533fe445991ac8ca3b3bb8df4a11c573f6f",
 "gate": "Compatibility schema and v0.7 fixtures",
 "note": "The hosted check runs cited here were produced on f9c8bc8f. The only tracked file that changed between that commit and the one this receipt names is release-checklists/0.8.2.md, which ships in neither the wheel nor the sdist (verified against the built wheel's name list), so no code the hosted lanes exercised differs.",
 "result": "'extras matrix / exact candidate' and 'clean-wheel install + import sweep' are both success on this SHA; cross_version_fixtures_test passes locally (2 passed, 5 subtests)"
}
