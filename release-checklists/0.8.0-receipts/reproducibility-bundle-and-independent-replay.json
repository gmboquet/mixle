{
  "artifact": "mixle.release_gate_receipt/v1",
  "candidate_commit": "0febc9c2905815d8f4581a95a1311b937ec4666f",
  "command": "python scripts/run_repro_entry.py --bundle release-checklists/0.8.0-repro-bundle.json --entry <each of gallery-univariate, gallery-structured, production-provenance, scaling-backend>, in a clean virtualenv installing only the built 0.8.0 wheel plus numpy==2.4.6 scipy==1.17.1",
  "date": "2026-09-04",
  "evidence_digest": "353f3de273291c398be7edd45e27dcce782d865154afe242a06ab9f11151d63f",
  "gate": "Reproducibility bundle and independent replay",
  "note": "All four local-tier entries (gallery-univariate, gallery-structured, production-provenance, scaling-backend) reproduced byte-for-byte from the built 0.8.0 wheel installed into a clean virtualenv with the bundle's pinned arithmetic (numpy 2.4.6 / scipy 1.17.1); every entry's executing_artifact.installed_distribution and .installed_content_verified were true, confirming wheel-based execution rather than a shadowing source tree. gallery-univariate previously printed raw fitted floats that differed by ULPs per CPU/BLAS and reproduced on no platform but the one it was pinned on (macOS arm64 gave 63bd1ee7, Linux arm64 gave 7b1b2378, and the old pinned value was 3e54938b -- three distinct hashes); this was verified independent of the M-step location fix, since the D-0210 candidate dd167be2 produced the same 63bd1ee7 on this machine. examples/gallery_univariate_example.py now rounds every printed float to 6 significant figures (well above ULP noise, well below any real fit difference) and reproduces identically across macOS arm64 and Linux arm64 (9c9292b4). Replay performed by the release owner's agent, extending the D-0210 independent-replay override to this candidate; the mechanical independence (separate build, separate clean install, separate extracted source tree, no shared state) is real, though it is not evidence of independence from the implementation author's own blind spots.",
  "result": "4 of 4 bundle entries reproduced byte-for-byte; installed_content_verified=true for all 4."
}
